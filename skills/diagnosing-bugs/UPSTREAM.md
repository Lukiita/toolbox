# Upstream - diagnosing-bugs

- **Repo**: https://github.com/mattpocock/skills (MIT), `skills/engineering/diagnosing-bugs/`
- **Vendored base**: 1.2.3, upstream commit `6654f6b60cd9d5be8b54c6fafe44346dabeb3b76`, on the shared **`vendor/mattpocock-skills`** branch (imported 2026-08-27)

## Local delta

1. `metadata.origin` frontmatter.
2. Phase 5 handoff: "no correct seam for the regression test" is flagged to a design review (the `arch-review` agent, on `codebase-design`'s vocabulary) instead of upstream's `improve-codebase-architecture`, which is not carried here.

## How to update

Same procedure as tlc (`skills/tlc-spec-driven/UPSTREAM.md`), on the shared vendor branch.
